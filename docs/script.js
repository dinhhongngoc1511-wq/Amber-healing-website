const toggle=document.querySelector('.menu-toggle');const mobile=document.querySelector('.mobile-nav');if(toggle&&mobile){toggle.addEventListener('click',()=>{const open=mobile.classList.toggle('open');document.body.classList.toggle('menu-open',open);toggle.setAttribute('aria-expanded',String(open));toggle.textContent=open?'×':'☰'});mobile.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobile.classList.remove('open');document.body.classList.remove('menu-open');toggle.textContent='☰'}))}
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const form=document.querySelector('#consultForm');if(form){const requested=new URLSearchParams(location.search).get('need');if(requested){const select=form.querySelector('[name="need"]');if(select&&[...select.options].some(o=>o.value===requested))select.value=requested}form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);const msg=`Chào Amber Healing, mình là ${data.get('name')||''}. Mình muốn được tư vấn về ${data.get('need')||'hành trình phù hợp'}. ${data.get('message')||''}`;navigator.clipboard?.writeText(msg);const note=document.querySelector('#formStatus');note.textContent='Thông tin đã được chuẩn bị. Hãy quét mã Zalo và dán tin nhắn để gửi cho Ms. May.';note.scrollIntoView({behavior:'smooth',block:'center'})})}
